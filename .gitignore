# Промежуточная SSR-сборка для пререндера (не деплоится)
dist-ssr/

# Зависимости ставятся через npm ci (исторически часть node_modules попала в git —
# старые файлы остаются под контролем, новые не добавляем)
node_modules/

# Логи и локальный мусор
*.log
.DS_Store
